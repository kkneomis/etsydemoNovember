<table class="table table-bordered table table-striped">
  <tr>
    <th class= "center">Image</th>
    <th class= "center">Name</th>
    <th class= "center">Condition</th>
    <th class= "center">Price</th>
    <th class= "center">Action</th>
  </tr>

   <% @listings.each do |listing| %>
      <tr>
        <td><%= image_tag listing.image.url(:thumb) %></td>  
        <td><%= listing.name %></td>  
        <td><%= listing.condition %></td>  
        <td><%= number_to_currency(listing.price) %></td> 
        <td>
             <div class="btn-group">
                <button type="button" class="btn btn-success ">Action</button>
                <button type="button" class="btn btn-success dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                  <span class="caret"></span>
                  <span class="sr-only">Toggle Dropdown</span>
                </button>
                <ul class="dropdown-menu" role="menu">
                  <li><a href="#"><%= link_to "View", listing, class: "btn btn-link" %></a></li>
                  <li><a href="#"><%= link_to 'Edit', edit_listing_path(listing), class:"btn btn-link" %></a></li>
                  <li><a href="#"> <%= link_to 'Destroy', listing, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-link" %></a></li>
               </ul>
            </div>

        </td>
      </tr>
    <% end %>
</table>
 <% if user_signed_in? %>
  <%= link_to 'New Listing', new_listing_path, class: "btn btn-link" %>
<% end %>

 
