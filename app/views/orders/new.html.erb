<h1>Book Request</h1>

<div class="row">
  <div class="col-md-3"><%= image_tag @listing.image_url(:thumb) %></div>
 <div class="col-md-9">
   <h3 > <%= @listing.name.upcase %></h3>
      <p><strong>Author:</strong> <%= @listing.author %></p>
      <p><strong>Price:</strong> <%= number_to_currency(@listing.price) %></p>
      <p><strong>Condition:</strong><%= @listing.condition %></p>
      <p><strong>Course:</strong><%= @listing.course %></p>
      <p>Owned by:<%= @listing.user.name %></p>
  </div>
<br>

<div class="container">
  <h3> Send a message to <%= @listing.user.name %> to let him/her know that you would like to purchase this book </h3>
  </div>

  
  <div class="alert alert-warning" role="alert">  <p> <span class="glyphicon glyphicon-warning-sign" aria-hidden="true"></span>Be sure to include a phone number or email address where <%= @listing.user.name %> can contact you <p></div>

<%= render 'form' %>

  
  <%= link_to 'Back', listings_path, class: "btn btn-link" , data: { disable_with: "back" } %>
